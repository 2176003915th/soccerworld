<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="MemberName">
<!--    <sql id = 'sql_selectAll' >-->
<!--        select * from Member-->
<!--    </sql>-->
    <select id="selectMember" parameterType="String" resultType="idusw.soccerworld.domain.entity.MemberEntity">
        select * from Member where id =#{id}
    </select>
</mapper>